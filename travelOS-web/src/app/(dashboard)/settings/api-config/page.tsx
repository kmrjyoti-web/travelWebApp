'use client';
import { ApiConfigPage } from '@/features/settings';

/**
 * Route: /settings/api-config
 * Renders the API Integrations settings page inside the dashboard layout.
 */
export default function SettingsApiConfigPage() {
  return <ApiConfigPage />;
}
