/**
 * React AICIcon component barrel export.
 */

export { AICIcon } from "./AICIcon";
export type { AICIconProps } from "./AICIcon";

// Re-export core types for convenience so consumers don't need to
// depend on @coreui/ui directly for common type imports.
export type {
  AICIconProvider,
  AICIconSize,
} from "@coreui/ui";
