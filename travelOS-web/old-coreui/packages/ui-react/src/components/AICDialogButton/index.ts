/**
 * React AICDialogButton component barrel export.
 */

export { AICDialogButton } from "./AICDialogButton";
export type { DialogButtonProps } from "./AICDialogButton";

// Re-export core types for convenience so consumers don't need to
// depend on @coreui/ui directly for common type imports.
export type {
  DialogType,
} from "@coreui/ui";
