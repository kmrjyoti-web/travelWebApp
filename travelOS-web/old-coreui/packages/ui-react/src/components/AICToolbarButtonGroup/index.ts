/**
 * React AICToolbarButtonGroup component barrel export.
 */

export { AICToolbarButtonGroup } from "./AICToolbarButtonGroup";
export type { ToolbarButtonGroupProps } from "./AICToolbarButtonGroup";

// Re-export core types for convenience so consumers don't need to
// depend on @coreui/ui directly for common type imports.
export type { ToolbarButtonGroupItem } from "@coreui/ui";
