/**
 * React AICSwitch component barrel export.
 */

export { AICSwitch } from "./AICSwitch";
export type { SwitchProps } from "./AICSwitch";

// Re-export core types for convenience so consumers don't need to
// depend on @coreui/ui directly for common type imports.
export type { SwitchSize } from "@coreui/ui";
